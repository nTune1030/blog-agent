================================================================================
           API CREDENTIALS SETUP GUIDE — Step-by-Step Walkthrough
           Project: blog-agent · Author: NTUNE1030
================================================================================

This guide walks you through setting up API credentials for all three
platforms so the Promotion Agent can post automatically.

================================================================================
PLATFORM 1: REDDIT (via PRAW or Browser Fallback)
================================================================================

IMPORTANT: Reddit has changed their API access policy (2024+). Creating new
"script" apps now requires API access approval, which may be denied for
non-moderation use cases. Two posting methods are available:

  METHOD A: PRAW API (requires approved Reddit app) — preferred
  METHOD B: Browser Fallback (Playwright) — works without API approval

⚠️  NOTE: Reddit's "Devvit" platform (developers.reddit.com) is for building
    interactive apps that run INSIDE Reddit posts (games, polls, widgets).
    It is NOT for programmatic content posting. Devvit cannot be used to
    submit text/link posts to subreddits. Our browser fallback is the correct
    approach for automated posting.

────────────────────────────────────────────────────────────────────────────────
METHOD A: PRAW API (Recommended if approved)
────────────────────────────────────────────────────────────────────────────────

PREREQUISITE: You need a Reddit account AND API access approval.

STEP 0: Request API Access (NEW — required as of 2024)
  1. Go to https://www.reddit.com/r/reddit.com/wiki/api
  2. Read the Responsible Builder Policy
  3. Click the link to submit an API access request
  4. Select "I'm a Developer" and "I want to register to use the Reddit API"
  5. Fill in the form:
     - Benefit/purpose: Sharing open-source project updates with developer
       communities. Posts are technical and community-focused.
     - Detailed description: Personal script that posts technical project
       announcements to specific subreddits. Uses PRAW. Posts at most once
       per week per subreddit. No automated commenting, voting, or cross-posting.
     - What's missing from Devvit: Devvit builds in-Reddit UI apps. My use
       case is a server-side personal script for scheduled posting.
     - Source code link: https://github.com/NTUNE1030/blog-agent
     - Subreddits: r/Python, r/netsec, r/HowToHack
  6. Wait for approval (may take hours to days)
  7. You'll receive an email at the address you provided

STEP 1: Create a Reddit App (after API access is approved)
  1. Log in to Reddit at https://www.reddit.com/
  2. Go to https://www.reddit.com/prefs/apps
  3. Scroll down and click "create another app..." (or "create an app")
  4. Fill in the form:
     - name:           PromotionAgent
     - App type:        Select "script" (NOT "web app")
     - description:     Agent for posting project promotions
     - about url:       (leave blank)
     - redirect uri:    http://localhost:8080  (required but unused for script apps)
  5. Click "create app"

STEP 2: Copy Your Credentials
  After creating the app, you'll see:
  
  ┌─────────────────────────────────────────────────┐
  │  PromotionAgent                                  │
  │  personal use script                             │
  │                                                   │
  │  client_id:     abc123XYZ_defghi     ← UNDER the │
  │                                  app name, small  │
  │  secret:       AbCdEf123456GhIjKl   ← Listed as  │
  │                                  "secret"         │
  └─────────────────────────────────────────────────┘

  - client_id     = the string directly under the app name
  - client_secret = the string labeled "secret"

STEP 3: Fill In promotion_profile.json
  Open promotion_profile.json and update api_credentials.reddit:

  "api_credentials": {
    "reddit": {
      "client_id": "PASTE_YOUR_CLIENT_ID_HERE",
      "client_secret": "PASTE_YOUR_CLIENT_SECRET_HERE",
      "username": "YOUR_REDDIT_USERNAME",
      "password": "YOUR_REDDIT_PASSWORD",
      "user_agent": "PromotionAgent/1.0 by YOUR_REDDIT_USERNAME"
    }
  }

  ⚠️  SECURITY: Your Reddit password is stored in plaintext. Consider
  using a Reddit account specifically for bot/automation purposes, not
  your personal main account.

────────────────────────────────────────────────────────────────────────────────
METHOD B: Browser Fallback (Playwright — works without API approval)
────────────────────────────────────────────────────────────────────────────────

If your API access request is denied or you don't want to wait, you can
use browser-based posting via Playwright. This only requires your Reddit
username and password — no API app needed.

PREREQUISITE: You need a Reddit account and Playwright installed.

STEP 1: Install Dependencies
  pip install playwright playwright-stealth
  python -m playwright install chromium


STEP 2: Fill In promotion_profile.json
  Open promotion_profile.json and update api_credentials.reddit.
  You ONLY need username and password (leave client_id and client_secret empty):

  "api_credentials": {
    "reddit": {
      "client_id": "",
      "client_secret": "",
      "username": "YOUR_REDDIT_USERNAME",
      "password": "YOUR_REDDIT_PASSWORD",
      "user_agent": "PromotionAgent/1.0 by YOUR_REDDIT_USERNAME"
    }
  }

STEP 3: Two Browser Posting Modes

  Semi-automatic (RECOMMENDED — you review and click Post):
    from tools.reddit_browser_ops import open_reddit_post_form
    open_reddit_post_form("Python", "My Title", "My body text")
    # Browser opens, form is filled, YOU click "Post"

  Fully automatic (CRITICAL risk — requires YES confirmation):
    from tools.reddit_browser_ops import post_to_reddit_browser
    post_to_reddit_browser("Python", "My Title", "My body text")
    # Browser opens, logs in, fills form, and clicks Post automatically

  Or via the unified post_to_reddit tool:
    from tools.posting_ops import post_to_reddit
    post_to_reddit("Python", "My Title", "My body text", use_browser=True)

  ⚠️  NOTE: Browser mode may trigger Reddit's CAPTCHA or 2FA on first login.
  If this happens, use semi-automatic mode (open_reddit_post_form) so you
  can handle the CAPTCHA manually in the browser window.

  ⚠️  NOTE: Low-karma accounts may be blocked from posting in some
  subreddits. Consider building up karma by participating in discussions
  before posting promotional content.

================================================================================
PLATFORM 2: TWITTER/X (via Tweepy)
================================================================================

PREREQUISITE: You need a Twitter account and a phone number for
developer access.

STEP 1: Apply for a Developer Account
  1. Go to https://developer.twitter.com/en/portal/petition/essential-info
  2. Sign in with your Twitter account
  3. Fill out the application:
     - What is your primary reason for using Twitter APIs?
       → "Building tools to automate posting project updates"
     - Will you make your app content available to government entities?
       → Answer honestly (usually No)
  4. Agree to the Developer Agreement
  5. You may get instant approval, or it may take hours/days

STEP 2: Create a Project and App
  1. Go to https://developer.twitter.com/en/portal/dashboard
  2. Click "+ Create Project"
  3. Project name: PromotionAgent
  4. Use case: "Building tools to automate posting"
  5. Project description: "Automated project promotion agent"
  6. Click "+ Add App"
  7. App name: promotion-agent
  8. Environment: Development

STEP 3: Generate API Keys
  After creating the app, you'll see your Keys and Tokens:

  REQUIRED — API Keys (generate if not shown):
  - api_key            = "API Key" (also called Consumer Key)
  - api_secret         = "API Key Secret" (also called Consumer Secret)

  REQUIRED — Access Tokens (click "Generate" if not shown):
  - access_token       = "Access Token"
  - access_token_secret = "Access Token Secret"

  OPTIONAL — Bearer Token (for API v2 read-only):
  - bearer_token       = "Bearer Token"

STEP 4: Set App Permissions to READ AND WRITE
  ⚠️  CRITICAL: By default, apps are Read-Only. You MUST change this.

  1. Go to your app's "Settings" tab
  2. Scroll to "App permissions"
  3. Change from "Read" to "Read and Write"
  4. Save
  5. REGENERATE the Access Token and Access Token Secret
     (the old ones only have Read permission)

STEP 5: Fill In promotion_profile.json
  Open promotion_profile.json and update api_credentials.twitter:

  "api_credentials": {
    "twitter": {
      "api_key": "PASTE_YOUR_API_KEY",
      "api_secret": "PASTE_YOUR_API_SECRET",
      "access_token": "PASTE_YOUR_ACCESS_TOKEN",
      "access_token_secret": "PASTE_YOUR_ACCESS_TOKEN_SECRET",
      "bearer_token": "PASTE_YOUR_BEARER_TOKEN"
    }
  }

================================================================================
PLATFORM 3: DISCORD (via Webhooks)
================================================================================

PREREQUISITE: You need "Manage Webhooks" permission in the target
Discord server.

STEP 1: Create a Webhook
  1. Open Discord and go to your target server
  2. Right-click the channel where you want to post
     (e.g., #tools, #projects, #showcase)
  3. Click "Edit Channel"
  4. Go to "Integrations" → "Webhooks"
  5. Click "New Webhook"
  6. Name it: Promotion Agent
  7. Choose an avatar (optional)
  8. Click "Copy Webhook URL"
  9. Click "Save Changes"

STEP 2: Fill In promotion_profile.json
  Open promotion_profile.json and update api_credentials.discord:

  "api_credentials": {
    "discord": {
      "webhook_urls": [
        "https://discord.com/api/webhooks/123456789/AbCdEfGhIjKlMnOpQrStUvWxYz"
      ]
    }
  }

  To post to multiple channels, add multiple webhook URLs:
  "webhook_urls": [
    "https://discord.com/api/webhooks/CHANNEL_1_WEBHOOK",
    "https://discord.com/api/webhooks/CHANNEL_2_WEBHOOK"
  ]

================================================================================
AFTER SETUP: Verify Everything Works
================================================================================

1. Check readiness:
   python promote.py --readiness

   You should see:
   Reddit:    READY TO POST ✅ (API)     — if PRAW credentials are set
   Reddit:    READY (Browser) 🌐         — if only username/password set
   Hacker News: MANUAL ONLY
   Discord:   READY TO POST ✅
   Twitter/X: READY TO POST ✅

2. Check Reddit browser fallback readiness:
   python -c "from tools.reddit_browser_ops import check_reddit_browser_ready; print(check_reddit_browser_ready())"

3. Dry-run posting (no actual posts):
   python promote.py --post

4. Actually post (requires YES confirmation per platform):
   python promote.py --post --confirm

================================================================================
SECURITY NOTES
================================================================================

⚠️  promotion_profile.json contains plaintext API secrets and passwords.
    ADD IT TO .gitignore — never commit this file to a public repo!

⚠️  Reddit: Consider using a dedicated bot account, not your personal one.
    Reddit may shadowban accounts that post too frequently or appear automated.

⚠️  Reddit API Access: As of 2024, Reddit requires API access approval for
    new apps. If denied, use the browser fallback (Playwright) method.

⚠️  Twitter: Respect rate limits. Free tier allows 1,500 tweets/month.
    Do not spam — post once per project launch, not repeatedly.

⚠️  Discord: Webhook URLs are like passwords — anyone with the URL can post.
    Keep them private and rotate if leaked.